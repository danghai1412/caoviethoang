<table class="table table-bordered bg-light p-4 mt-3 mb-3 text-center">
   <tr>
      <th class="text-primary" colspan="5">Control toys list</th>
      <th>
         <a class="btn btn-primary" href="/marvel/add">ADD</a>
         <a class="btn btn-success" href="/marvel/drop"
            onclick="return confirm('Are you sure to drop all items ?');">DROP</a>
      </th>
   </tr>
   <tr>
      <th>Name</th>
      <th>Origin</th>
      <th>Material</th>
      <th>Image</th>
      <th>Price</th>
      <th>Status</th>
      <th>Menu</th>
   </tr>
   <tr>
      <th colspan="6">
         <select class="form-control" onchange="window.location.href=this.value">
            <option value="">Sort by name</option>
            <option value="/Marvel/sort/asc">Ascending</option>
            <option value="/Marvel/sort/desc">Descending</option>
         </select>
      </th>
      <th colspan="1">
         <form action="/Marvel/search" method="post">
            <div class="form-group">
               <input class="form-control" type="search" name="name" placeholder="Search by name">
            </div>
         </form>
      </th>
   </tr>
   {{#each marvel }}
   <tr>
      <td>{{name}}</td>
      <td>{{origin}}</td>
      <td>{{material}}</td>
      <td>
         <a href="/marvel/detail/{{_id}}">
            <img src="{{image}}" width="100" height="100">
         </a>
      </td>
      <td>{{price}}</td>
      <td>{{status}}</td>

      <td>
         <a class="btn btn-warning" href="/marvel/edit/{{_id}}">EDIT</a>
         <a class="btn btn-danger" href="/marvel/delete/{{_id}}">DELETE</a>
      </td>
   </tr>
   {{/each}}
</table>